import Layout from "../components/Layout";
import LandingPageSection from "../containers/Home";

const Home = () => {
  return (
    <Layout>
      <LandingPageSection />
    </Layout>
  );
};

export default Home;
